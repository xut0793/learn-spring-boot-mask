<?xml version="1.0" encoding="UTF-8"?>
<!--
  教程配套代码模块。

  刻意不挂进根 pom 的 <modules>：parent 直接指向 spring-boot-starter-parent，
  所以在项目根执行 `mvn clean test` 不会构建本模块，原有构建行为完全不受影响。

  单独构建本模块：
      mvn -f mask-tutorial/pom.xml test
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.7</version>
        <relativePath/>
    </parent>

    <groupId>com.learn.mask</groupId>
    <artifactId>mask-tutorial</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <name>mask-tutorial</name>
    <description>Hands-on code that accompanies the docs/ tutorial chapters</description>

    <properties>
        <java.version>21</java.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <!-- 第 5 章：从 SecurityContextHolder 解析角色 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <!-- 第 5 章：HeaderRoleFilter 需要 OncePerRequestFilter -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc</artifactId>
        </dependency>
        <!-- 第 8 章：展示脱敏结果缓存 -->
        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
        </dependency>
        <!-- 第 8 章：脱敏指标打点 -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
